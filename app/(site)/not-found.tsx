import { NotFoundHero } from "@/components/site/not-found-hero";

/** 404 for notFound() within the site shell (keeps header and footer). */
export default function NotFound() {
  return <NotFoundHero />;
}
